build:
  box: alpine
  docker: true
  steps:
    - script:
        name: Install docker command
        code: apk --no-cache add docker   
    - script:
        name: Install curl command
        code: apk --no-cache add curl      
    - script:
        name: Install fn cli
        code: |
          curl -L -o ${WERCKER_ROOT}/fn https://github.com/fnproject/cli/releases/download/0.5.28/fn_linux

          chmod +x ${WERCKER_ROOT}/fn
          ${WERCKER_ROOT}/fn version
    - script:
        name: Build Java function
        code: ${WERCKER_ROOT}/fn -verbose build
